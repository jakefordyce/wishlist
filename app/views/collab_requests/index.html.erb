<h3>Collaboration Requests</h3>

<table>
  <thead>
    <tr>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @collab_requests.each do |collab_request| %>
      <tr>
        <td><%= collab_request.user.username %></td>
        <td>List: <%= collab_request.list.name %></td>
        <td><%= link_to 'Accept', collaborations_path(collab_request: collab_request), method: :post %></td>
        <td><%= link_to 'Deny', collab_request, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<h3>Collaboration Invites</h3>

<table>
  <thead>
    <tr>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @collab_invites.each do |collab_invite| %>
      <tr>
        <td><%= collab_invite.list.user.username %></td>
        <td>List: <%= collab_invite.list.name %></td>
        <td><%= link_to 'Accept', collaborations_path(collab_request: collab_invite), method: :post %></td>
        <td><%= link_to 'Deny', collab_invite, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
