
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">


    <%= link_to "Wishlist", root_path, class: "navbar-brand text-light" %>


    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto text-right">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to pending_requests, collab_requests_path, class: "nav-link text-light"%>
          </li>
          <li class="nav-item">
            <%= link_to current_user.username, profile_path(current_user) , class: "nav-link text-light"%>
          </li>
          <li class="nav-item">
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "nav-link text-light" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Signup", new_user_registration_path, class: "nav-link text-light" %>
          </li>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link text-light" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign in with Google", user_google_oauth2_omniauth_authorize_path, method: :post %>
          </li>
        <% end %>
      </ul>
    </div>

  </nav>

